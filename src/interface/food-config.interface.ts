export interface FoodTemplateConfig {
  titleColor: string,
  subTitleColor: string,
  themeColor: string
}

export interface FoodContentConfig {
  logoImage: string,
  titleText: string,
  frames: Array<any>
}